export const CLOUDCLI_WORDMARK_FONT_FAMILY =
  'ui-sans-serif, system-ui, sans-serif, Apple Color Emoji, Segoe UI Emoji, Segoe UI Symbol, Noto Color Emoji';

// User-facing product name (가재코드 = Gajae Code). Functional identifiers
// (npm package, `cloudcli-` MCP prefixes, `.cloudcli/` paths) are intentionally
// NOT rebranded — only the visible identity.
export const BRAND_NAME = '가재코드';
